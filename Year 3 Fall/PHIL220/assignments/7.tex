\documentclass{article}
\usepackage{../../../LaTeX-Preamables/Base}


\begin{document}

\section*{PHIL200 - Assignment 7}

\begin{enumerate}
    \item
          $
              \exists x \, (F(x, l) \wedge L(m, x))
          $

    \item
          $
              \forall x \, (U(x, l) \rightarrow A(x))
          $

    \item
          $
              \forall x \, (L(c, x) \rightarrow x = c)
          $

    \item
          $
              \forall x \, \neg M(x, x)
          $

    \item
          $
              \forall x \, ((x \ne b) \rightarrow L(b, x))
          $

    \item
          $
              \forall x \, (D(x) \rightarrow \exists y \, (C(y) \wedge L(x, y)))
          $

    \item
          $
              \forall x \, (D(x) \rightarrow \exists y \exists z \, (C(y) \wedge C(z) \wedge y \ne z \wedge L(x, y) \wedge L(x, z)))
          $

    \item
          $
              \forall x \forall y \, (S(y) \rightarrow \neg L(x, y))
          $
\end{enumerate}


\end{document}